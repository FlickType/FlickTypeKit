language: swift
osx_image: xcode11.1

script:
  - |
    set -o pipefail && \
    xcodebuild \
    -project "FlickTypeKit Sample (Swift)/FlickTypeKit Sample.xcodeproj" \
    -scheme "FlickTypeKit Sample WatchKit App" \
    build | xcpretty
